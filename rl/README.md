# 코윅윅 잔차 RL — 1단계 (속도 잔차)

**세팅 완료 + 동역학 랜덤화 + 전수검수 P0 계약 반영 (2026-08-21).**
학습은 8장 명령 한 줄로 시작한다.

> 🔴 **계약 버전 `official_cost_v1+referee_finish_v1` (2026-08-21 밤).**
> 종료 = 공식 결승선(심판 판정 이식), 완주 = terminated, 보상 = 공식 비용
> 그대로, gamma 0.9995, **학습 콘 분포 = 평가 콘 분포**(랜덤 + cone6 고정),
> pose 신선도 강제, 텔레포트 후 관측 리셋, 평가 fail-closed,
> quick_gate 시간 측정.
> **이 계약 이전의 모델·버퍼(s1~s6)는 다른 게임을 배운 것이라 재사용 금지.**
> 근거: `Downloads/2026-08-21_RL잔차전수검수_답변.md` (P0-1~8).
> 회귀 테스트: `test_dyn.py` + `test_env_dyn.py` + **`test_contract.py`**.

---

## 1. 무엇을 학습하는가

`racer-v64` 위에 **속도 잔차 1차원**만 얹는다. 조향은 건드리지 않는다.

근거는 실측이다. 주행 중 1375틱 상한 귀속:

| 상한 | 비중 |
|---|---|
| `V_MAX` (0.90) | 34.6 % |
| 콘 회피 0.45 그룹 | 26.5 % |
| 라이다 프리브레이크 (0.40/0.55) | 30.8 % |
| 콘 접근 0.60 | 5.7 % |
| **횡가속 곡선** | **1.7 %** |

**코너 물리는 94.7 % 구간에서 제약이 아니다.** 우리를 느리게 하는 것은
보수적 상한 상수들이므로, 학습할 자유도는 "어떤 조건에서 상한을 얼마나 풀까"다.
조향 잔차는 결승선 신뢰성 게이트를 통과한 뒤에만 붙인다.

---

## 2. driver.py 를 건드리지 않는다

`/speed` 퍼블리셔만 가로채 `base + residual` 을 대신 낸다.

```
action 0  →  잔차 0  →  정확히 racer-v64
```

검증 완료:
- `TEST 1  zero residual  |cmd-base| max = 0.00e+00`  (PASS)
- 러너를 잔차 0 으로 태워 **심판 채점 3/3, 평균 51.24, 전부 페널티 5.0
  @ (0.90~0.91, 4.8x)** — v64(51.05~51.18) 와 같은 패턴·같은 자리

`driver.py` 에는 계측 dict 한 곳(`self.last_tick`)만 추가돼 있다. 아무도 읽지
않는 순수 대입이고, **동작 불변을 실측으로 확인**했다:
- `score_now` ok 235 (v64 와 완전 동일)
- 예선 3런 평균 51.05, 페널티 3/3 정확히 5.0

---

## 3. 관측 — 그라운드트루스를 하나도 넣지 않는다

정책이 보는 것은 **베이스 제어기가 카메라·라이다에서 이미 뽑는 값 + 자기 명령
이력**뿐이다. 절대좌표·오도메트리는 물론이고 `route.json` 유래 값(경계까지
여유 등)도 뺐다 — **실차에 없는 정보에 의존하면 sim-to-real 이 죽는다.**

한 틱 23차원 × 최근 3틱 = **69차원**:
```
speed/V_MAX, steer/MAX_STEER, aim, carea, arc_safe, fmin,
lbias, pass_hold, pass_side, ncone, frame_age, green,
prev_a                                   <- 직전 행동 (정규화 잔차)
+ 활성 상한 one-hot 10종
  (vmax, alat, cone_slow, cone_near, arc_short, checker, lidar1, lidar2, sharp, crawl)
```
`prev_a` = 자기 **행동 이력**(직전 잔차). 스무딩 벌점은 P0-6 에서 보상과
함께 제거됐지만 prev_a 는 남긴다 — GPT 4.3 이 가장 중요한 누락으로 꼽은
"직전 최종 명령 이력"의 최소 형태이고, 부분관측(플랜트 추첨)을 줄인다.
`speed` 는 **base 명령**이라 잔차가 얹히기 전 값이므로 대체가 안 된다.
텔레포트·STALE 뒤에는 학습·배포 모두 hist 와 prev_a 를 리셋한다 (P0-4/P1-10).
⚠️ `run_policy.py` 가 **똑같이** 채운다 (`test_env_dyn` D4 가 소스로 검사).
상한 one-hot 이 중요하다: 같은 0.45 라도 콘 회피인지 결승 밴드인지 구분하지
못하면 정책이 같은 행동을 낸다.

경계까지 여유(`room`)는 **보상 계산에만** 쓴다. 학습 중 시뮬에서만 필요한 값이다.

---

## 4. 행동

```
a ∈ [-1, +1]  →  잔차 = a × 0.35 m/s
최종 명령 = clip(base + 잔차, 0, 1.40)     ← 하드 클립
```
대칭인 이유: `a=0` 이 정확히 v64 라서 SAC 초기 정책이 검증된 지점에서 출발하고,
실차에서 이상하면 잔차만 끄면 된다.

---

## 5. 보상 — 공식 비용 **그대로** (`official_cost_v1`, 전수검수 P0-6)

```
r = −0.1 × (Δt + 5·I_off + 5·I_cone)
Δt = 0.05 × (1 + 페널티 회복에 쓴 추가 틱)     ← 심판의 시계는 회복 중에도 돈다
```
- **진행 보상 없음 (K_PROG=0).** "랩 거리가 고정이라 K·D 는 상수" 논증은
  할인 때문에 성립하지 않고(Σγᵗ·Δs 는 언제 전진했느냐에 달렸다), 결승선
  놓침을 모델링하는 순간 raw 진행 보상은 추가 한 바퀴 30.5 m 를 30.5 초
  이득으로 계산해 **실제 50 초 하방을 상쇄**해 버린다
- **배리어·스무딩 항 없음.** 최소화 대상이 공식 점수 그 자체가 되도록,
  GPT 권고대로 가장 안전한 형태부터 시작한다
- 완주는 `terminated` 라 그 뒤 가치가 부트스트랩되지 않는다 → return 이
  정확히 "이 랩의 남은 공식 비용"이다
- 검증: `test_contract.py` S1c — 한 랩의 Σr/W 가 공식 비용(t_s + 5·N)과
  1e-6 안에서 일치

**진행량 `ds` 는 진단값으로만 남는다** (선분 사영, 순간이동 스텝은 0).
보상에는 들어가지 않는다.

### 심판 동등성 — 적대 리뷰가 잡은 4건 (2026-08-22, 전부 수정)
보상이 유일한 학습 신호라 **판정이 심판과 조금이라도 다르면 정책은 그 차이를
학습한다.** P0 수정 직후 변경분을 적대 리뷰해 네 곳을 더 찾았다:

| | 종전 | 심판 | 크기 |
|---|---|---|---|
| **이탈** | 법선 사영 `\|e\|` | 최근접 wp 까지 **방사거리** | env 가 **한 방향으로만** 관대: 중앙 2.4 mm, p90 4.3 cm, 최대 31 cm (더 엄격한 표본 0/1032) |
| **결승 yaw** | `np.roll` → `yaw[N]=atan2(0,0)=0` | 중복 종점을 안 씀 (`% N`) | **89.4° 오차**, 하필 결승선 자리 |
| 히트 착지 | 차 기준 wp 앞 0.3 m | **콘 기준** wp 앞 0.3 m | ~0.17 m 뒤 → 콘 옆구리 재접촉 |
| 콘 rearm | 1 초 경과로 latch 해제 | **제자리 관측**으로 해제 | 심판이 안 주는 두 번째 +5 |

회귀 테스트: `test_contract.py` U6/U6b/U7 (이탈 판정 **1792 표본 전부 일치**).

### 종료 = 공식 결승선 (`referee_finish_v1`, P0-1)
심판의 판정을 1:1 이식했다 (`FinishDetector`): 앞점(pose+0.12 m)의 이동
선분이 결승 선분(inner[0]~outer[0], 폭 0.70 m)과 교차 + 누적 어드밴스 > N/2.
**선분 끝점 바깥으로 지나가면 랩이 인정되지 않고 계속 돈다** — 실측 1순위
병목(놓침 1회 ≈ 50초)이 이제 학습 안에 있다. 누적 arc 종료는 폐기했다
(그 기준에서는 이 사고가 원리적으로 존재할 수 없었다).

---

## 6. 텔레포트

심판은 이탈·콘히트 때 차를 **중심선 0.3 m 앞**으로 옮긴다 (주최측 확인:
*"네 강제 이동 합니다"*). env 도 **같은 규칙으로** 옮겨 학습 분포를 평가와 맞춘다.
그 스텝의 `ds` 는 **0 으로 만든다** — 안 그러면 정책이 "이탈해서 순간이동으로
전진"을 배운다.

---

## 7. 에피소드

```
리셋      심판처럼 **출발선 0.10 m 뒤**에 텔레포트 (lat ±0.10 m, yaw ±0.10 rad)
          + 콘 6개 재배치 (본선 분포)
워밍업    0.25 초 (5틱) — **차를 세워 둔 채** 새 프레임·last_tick 만 채운다
수집      **한 바퀴**, 정지 출발 → 공식 결승선. 안전망 150 초
```

### 워밍업 중에는 달리지 않는다 (P1-7, 2026-08-21 전수검수)
종전에는 워밍업 동안 베이스 제어기로 실제 주행했다 — **숨은 주행 구간**이다.
그 0.25 초는 심판이 시간을 재는 구간인데 정책 에피소드에서 사라졌고, 정책은
출발선이 아니라 이미 진행한 지점에서 시작했다. 배포에는 워밍업이 없다.
이제 워밍업은 speed 0 을 퍼블리시하며 프레임만 채우고, **주행은 첫 step 부터
정책이 본다** — 초록불 정지 출발이라는 심판·배포와 같은 상태에서.

### 랩 단위인 이유 (2026-08-21 사용자 지시로 8 초 슬라이스에서 변경)
**이탈하거나 콘을 쳐도 에피소드를 끊지 않는다.** 심판처럼 차를 중심선 앞
0.3 m 로 옮기고 **그 랩을 계속 돈다.** 그게 평가 조건 그 자체다.

바꾼 이유 세 가지:
1. **평가가 랩이다.** 공식 점수는 한 바퀴이고 페널티가 누적된다.
   8 초 슬라이스는 그 구조를 원리적으로 못 본다
2. **결승선 접근은 랩 스케일 사건**이고 그게 지금 1순위 병목이다. 슬라이스로는
   "랩의 끝으로서의 결승선"을 절대 경험하지 못한다 — 우연히 그 5 m 에 스폰될
   때만, 그것도 맥락 없이 본다
3. **리셋 자체가 인공물이다.** 텔레포트는 속도를 0 으로 만들고 워밍업 1.2 초는
   그걸 가리는 장치다. 랩 단위면 리셋이 60k 스텝당 **375 회 → 약 55 회**로 줄어
   인공물이 7 배 줄고 스텝률도 오른다

⚠️ **정직한 한계**: `gamma=0.995` 는 유효 지평이 약 10 초라, 랩으로 바꾼다고
"랩 전체를 내다보는" 추론이 자동으로 생기지는 **않는다.** 이득의 실체는
**분포 일치와 인공물 제거**다. 지평까지 늘리려면 `gamma` 를 따로 올려야 하고
그건 **별도 변경**으로 둔다 (한 번에 하나).

검증: `test_env_dyn.py` TEST G — 실주행 한 바퀴 877 스텝(43.9 초), `lap_s`
30.55 ≥ 30.50, **첫 페널티 236 스텝 → 총 877 스텝**(이탈 3건이 랩을 끊지 않음).
슬라이스로 돌리려면 `--no-lap --episode-s 8`.

---

## 7b. 동역학 랜덤화 (2026-08-21 구현, 기본 ON)

**에피소드마다 플랜트를 다시 뽑는다.** 근거 코드 `rl/dyn.py`.

### 왜
시뮬 구동계(`gz AckermannSteering`)는 **속도 소스**다 — 감속이 폴 1개
(~0.07 s, **≥4.5 m/s²** 실측) 안에 끝나고 3랩 실주행거리/명령거리 = **0.9968**.
**이 시뮬에서 감속은 공짜다.** 그 위에서 속도 잔차를 학습하면 정책은 반드시
"코너 직전까지 최고속 → 순간 감속"을 배운다. 실차 모터는 그렇게 안 선다 —
작년 RL 모델이 실차에서 전멸한 것이 정확히 이 형태다.
**콘 위치만 랜덤화해서는 이 계통 오차를 못 막는다.**

### 무엇을 (전수검수 P1-2/P1-4 로 속도 축만 남김)
| 축 | 범위 | 시뮬 참값 |
|---|---|---|
| 가속 한계 | 1.5~9.0 m/s² | 사실상 무한 (≥4.5 실측) |
| 감속 한계 | 1.0~9.0 m/s² | 〃 |
| 구동 지연 | 0~3 틱 (0~150 ms), 속도 명령에만 | 0 |

가속·감속을 따로 뽑는다 (미는 힘 ≠ 멈추는 힘). 텔레포트 시 구동 액추에이터는
0 으로 되돌린다 — 심판 텔레포트가 차를 세우기 때문이다(실측).

**제거된 축**: 관측 지연(정책 row 만 과거로 보내는 방식은 prev_a·frame_age·
base 가 서로 모순 — P1-2), 조향 서보·조향 지연(1단계 정책은 조향을 내지도
관측하지도 못해 보수성·분산만 만든다 — P1-4). 실차 실측(8/25) 후 맞는
위치에 다시 모델링한다.

⚠️ **정직한 주석 (P1-3)**: 상단 9.0 은 "한 틱 완전 추종"(18 m/s²)을 포함하지
않으므로 nominal passthrough 는 분포 밖이다. 범위 재설정은 실차 실측 후에.

### 어디에 안 켜는가
`run_policy.py`(배포)와 `eval_policy.py`(공식 채점)는 **참 플랜트**를 그대로
쓴다. 랜덤화는 학습 분포를 넓히는 도구지 평가 조건이 아니다. `--no-dyn` 으로
학습에서도 끌 수 있다 (대조군용).

### 검증 (전부 통과)
```bash
python3 rl/test_dyn.py        # 단위 20건, ROS 없이 몇 초
python3 rl/test_env_dyn.py    # 시뮬 인더루프 8건, 약 2분
```
| | 결과 |
|---|---|
| `dyn=False` 순수통과 | `\|cmd−base\| max 0.0` / `\|cmd_out−cmd\| max 0.0` (160 스텝) |
| 가감속 한계 위반 | **0** / 240 스텝 (단위테스트 200 에피소드도 0) |
| 플랜트가 무는 양 | 평균 `\|cmd−cmd_out\|` **0.041 m/s** |
| 에피소드당 진행 | 2.76 m (off) → **2.00 m** (on) — 약한 플랜트라 덜 간다 |
| 관측 차원 | 66 불변, 전부 유한 |

**끄면 즉시 순수 통과로 복귀한다** (TEST C). 되돌릴 수 있는 변경이다.

### 다음 후보 축 (지금은 안 넣는다)
모터 게인 오차, 조향 트림 바이어스. 실재하지만 **1차원 속도 잔차로는 보상할
수 없는 축**이라 보상 신호에 잡음만 더한다. 조향 잔차(2단계)에서 같이 열 것.

---

## 8. 실행 (승인 후)

```bash
. /opt/physicar/src/physicar-ros/deploy/sim/bashrc-append
cd ~/physicar_ws/coweek
python3 rl/train_speed.py --steps 150000 --out rl/runs/s7
```

스텝 수는 UTD 상향 대신 늘린다 (P1-1: gradient_steps 를 올리면 제어 주기가
밀려 플랜트가 바뀐다 — GPU 가 놀아도 신선한 데이터가 항상 낫다).

**예산**: 제어 20 Hz, RTF 1.0 이 하드 상한 → **시간당 72,000 스텝**.
시뮬 가속은 쓰지 않는다 — `driver.py` 가 `use_sim_time` 을 안 쓰므로 가속하면
시뮬 시간 기준 제어 주기가 줄어 **다른 플랜트**가 된다.

```
60k  ≈ 50분   |  144k ≈ 2시간  |  216k ≈ 3시간
```

**평가는 학습 곡선이 아니라 공식 채점기로만 한다:**
```bash
python3 rl/eval_policy.py --model rl/runs/s1/final.zip --runs 15 --baseline
python3 rl/eval_policy.py --model rl/runs/s1/final.zip --runs 15 --finals --baseline
```
같은 배치 안에서 정책 vs 잔차0 을 비교하고 Gate F/P/S 를 찍는다.

**실측 스텝률은 16.4~16.5 steps/s** (2026-08-21, 랜덤화 ON). 20 Hz 의 82 % 이고
차이는 전부 에피소드 리셋 오버헤드다 — **RTF 손실이 아니다** (학습 중 실측
`rtf 0.9948`). 그래서 60k 는 **약 61분**이다.

---

## 8b. 채택 기준 — **학습 결과를 보기 전에 못박는다**

이 프로젝트는 사후에 기준을 고르다 여러 번 데었다 (v67 은 예선 3개 기준을
다 통과하고 본선에서 뒤집혔고, v68 은 오프라인 게이트 셋을 다 통과하고 배치에서
정반대로 나왔다). 그래서 **숫자를 보기 전에** 적어둔다.

### 채택 조건 (전부 만족해야 채택)
같은 배치 안 비교, `--baseline` 필수. **예선맵과 본선 조건 둘 다** 돌린다.

| | 조건 |
|---|---|
| **Gate P 페널티 합** | 정책 ≤ 기준선 |
| **Gate S 평균 점수** | **짝지은 런별 차이의 평균 < 0 이고 95 % CI 상한 < 0** (동률·불확정 불합격 — P0-5) |
| **Gate C 콘 히트** | 정책 ≤ 기준선 |
| **Gate F 결승선 놓침** | 정책 ≤ 기준선 |

평가 자체가 **fail-closed** 다: 모델 로드 실패·predict 예외·잔차 전무·
stale trace·심판 비정상 종료·매니페스트 불일치 중 하나라도 걸리면 그 배치는
무효(exit 2)다. 종전에는 로드가 실패해도 잔차 0 이 동률 PASS 로 통과할 수
있었다 (P0-5).

### 미리 정한 해석 규칙
- **Gate F 는 증거가 아니라 지뢰선이다.** 기준선이 3 % 라 n=15 로는 못 가른다
  (0/15 여도 실제 9.5 % 가능). **늘면 즉시 기각**, 같거나 줄어도 "개선됐다"고
  쓰지 않는다
- **본선 단독 배치의 절대 수치는 안 믿는다.** 같은 v64 가 세 배치에서 페널티
  130/170/200 이었다. 그래서 **같은 배치 안의 정책 vs 기준선 차이**만 읽는다
- **예선맵만 통과한 것은 통과가 아니다** (v67 이 정확히 그 함정이었다)
- 기각이면 **파라미터를 만지작거려 재도전하지 않는다.** 왜 졌는지 기전을
  먼저 특정한다 — 이 프로젝트에서 성공한 변경은 전부 "이미 있는 측정을
  코드에 반영한 것"이었고, 실패한 것은 전부 "새 메커니즘 발명"이었다

### 채택돼도 남는 조건
- 배포는 `ACTIVE_MODEL` 파일 하나다. **8/25 실차에서 이상하면 지운다** —
  그 순간 v64 로 복귀한다. 이 롤백 경로를 현장 체크리스트에 넣을 것
- 학습은 **시뮬 플랜트 분포** 위에서 됐다. 실차 가감속을 재고 나면
  `dyn.py` 범위가 맞았는지 확인하고, 크게 어긋나면 **재학습이 아니라 잔차 0**
  으로 간다 (대회 당일에 학습할 시간은 없다)

---

## 9. 배포 / 롤백

`run.sh` 가 파일로 분기한다 (심판이 띄우는 프로세스엔 환경변수가 안 닿는다):

| `coweek/rl/ACTIVE_MODEL` | 동작 |
|---|---|
| 없음 (기본) | `driver.py` = **racer-v64** |
| `none` | 러너 + 잔차 0 (= v64, 검증용) |
| `.zip` 경로 | 러너 + 그 정책 |

**롤백은 파일 하나 지우는 것이다.**
원본 `run.sh` 백업: `/tmp/run_sh_ORIG_v64`

---

## 10. 아직 안 한 것 / 남은 위험

- ~~동역학 랜덤화 미구현~~ → **7b 로 해결** (2026-08-21). 단 **범위가 옳다는
  보증은 없다.** 실차 계수는 아직 안 재봤고 범위는 시뮬 참값과 상식적 열화폭
  으로 잡은 것이다. **8/25 실차에서 `turn_radius.py` 와 함께 실제 가감속을
  재고 범위를 다시 맞출 것** — 그때 `dyn.py` 상수 한 줄이면 된다
- ✅ **보상 수학 — 해결됨 (2026-08-21 밤, P0-6 반영).** 이 항목은 세 번
  뒤집혔다: ① "K_PROG 가 교환비보다 낮다"(슬라이스 시절) → ② "랩 단위면
  K_PROG·D 가 상수라 1.0 이 맞다"(정정) → ③ **그 정정이 틀렸다** (할인 때문에
  상수가 아니고, 보상 ds ≠ 랩 거리이고, dense/smooth 가 남고, 결승선 놓침을
  모델링하면 D 도 고정이 아니다). 현행: **K_PROG 없음, 보상 = 공식 비용
  그대로, gamma 0.9995** (5장). 코드·테스트(`test_contract.py` S1c) 반영 완료
- **에피소드 보상 분산의 상당 부분이 플랜트 추첨이다.** 초기 11 랩에서 랩타임이
  34.9~55.8 초로 21 초 흔들렸다. 가속 한계를 1.5~9.0 m/s² 에서 뽑으니 6 배
  차이이고 정책은 **플랜트를 관측할 수 없다.** SAC 는 1-스텝 TD 로 배우므로
  치명적이진 않지만, 성능 상한을 제한한다. **`quick_gate.py` 가 같은 seed 로
  짝지어 이 분산을 상쇄**하므로 비교는 그쪽으로 한다
- **결승선 놓침이 이제 학습 안에 있다** (P0-1) — 정책이 결승 앞에서 속도를
  올려 놓침을 만들면 추가 한 바퀴의 시간 비용을 그대로 문다. 단 **평가**는
  여전히 희귀 사건 문제다: 기준선 3 %, 1회 ≈ 50초라 15~30런으로는 판정이
  안 된다(0/30 이어도 실제 9.5 % 가능). **결승 전용 벤치로 수백 회 싸게
  재는 것**이 여전히 필요하다 (Gate F 는 지뢰선일 뿐)
- 조향 잔차는 2단계. 결승선 게이트 통과 전에는 붙이지 않는다
